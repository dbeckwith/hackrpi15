
/*
 * Sends request to other buddy to be a buddy
 *
 * requester: string
 * requestee: string
 */
function requestBuddy(body, callback) {
	var requester = body.requester;
	var requestee = body.requestee;
}

modules.export = requestBuddy;
